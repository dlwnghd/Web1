자바스크립트 전역함수
	- eval()
		문자열 형태로 수식을 전달받아서 수식을 계산한다.
		eval("2 * 3 + 4 * 6") == 30;

	- parseInt()
		문자열 타입을 정수 타입으로 변환한다.
		parseInt("32") === 32;

	- isNaN()
		NaN이라는 값인지 검사
		숫자가 아니면 true, 숫자면 false

	- isFinite()
		숫자면 true, 숫자가 아니면 false